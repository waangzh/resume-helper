interface:
  display_name: "岗位推荐与投递"
  short_description: "找精准岗位详情页并在确认后辅助投递"
  default_prompt: "Use $job-recommend to find exact job detail pages and apply only after I confirm selected roles."

policy:
  allow_implicit_invocation: true
